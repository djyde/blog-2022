---
import config from "../config";
const { umami } = config;
---

{
  import.meta.env.PROD && umami && (
    <script async defer data-website-id={umami} src="https://a.taonan.lu/ana.js" />
  )
}

{
  import.meta.env.PROD && (
    <script defer data-domain="lutaonan.com" src="http://psb.djyde.com/js/script.js"></script>
  )
}